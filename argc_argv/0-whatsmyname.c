#include <stdio.h>

/**
 * main - prints its name
 * @argc: number of argument
 * @argv: no argument to pass
 *
 * Return: always 0
 */
int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}

